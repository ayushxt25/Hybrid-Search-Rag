# Local Vite development backend API URL.
# Leave unset for production reverse-proxy builds so the app uses /api/v1.
# Do not put API keys, passwords, or provider secrets in VITE_* variables.
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
